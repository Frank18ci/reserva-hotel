import { PipeUbicacionImagesPipe } from './pipe-ubicacion-images.pipe';

describe('PipeUbicacionImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeUbicacionImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
